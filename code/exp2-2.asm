;字节写入
CODE SEGMENT
	ASSUME CS:CODE
START PROC FAR
	MOV AX,8000H
	MOV DS,AX
	MOV CX,010H
	MOV SI,0
	MOV AL,0			;仅使用低8位
AA0:
	MOV [SI], AL
	INC AL
	INC SI
	MOV [SI],BYTE PTR 00H;告诉编译器00H是8位的，而非16位
	INC SI
	LOOP AA0
	MOV AH,4CH
	INT 21H
START ENDP
CODE ENDS
	END START